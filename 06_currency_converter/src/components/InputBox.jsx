import React from 'react'

function InputBox({
  label,
  amount,
  onAmountChange,
  onCurrenyChange,
  currencyOption = [],
  selectCurrency = "usd",
  amountDisable = false,
  currencyDisable = fa lse
 className = "",}) 
 {
    


  return (
    <div className= {` `}>
      
    </div>
  )
}

export default InputBox
